/* $(function() {        
        // Create Bridge carousel
        if ($('.alt_gallery').exists()) {
          $(".alt_gallery").flexslider ({
                  animation: "slide",
                  //smoothHeight: true,
                  slideshow: false,
                  useCSS: false,
                  controlNav: false,
                  selector: ".slides > li",
          });

          $('ul.flex-direction-nav').css('height', 0);
      }
});
*/